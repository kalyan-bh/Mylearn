intiating git
git init
git config --global user.name "kane" / old user.name "kalyan"
git config --global user.email "---"
git congfig --list

git add . // add changes to the staging area
git restore --stage . // back to the unstage
git add --all // add changes to staging area
git reset // back to unstage
git status  ------>//to see any new changes
git branch //get the branch name
git commit -m "aaaaaaaaaaaaa"
git context branch "aaaaaaa"

git log  ----->//history of every commit
to get rid of log press "q"

git --help
git pull   //pull the changes from remote repository.
git remote add origin remote_repo_link
git push  
git push -u origin branch_name/master //used to push changes from local to remote repository. 
git fetch 

git checkout #unique id of commit stage#  ---->// to go to the previous changes
git checkout master  //to go to the master branch

git branch branch_name  //also creates new branch
git checkout new_branch_name //just creates new branch 
git checkout -b new_branch_name //short cut command to create branch and switch to the created one


git branch --all // to get all the branches list
git checkout -d branch_name  //command to delete branch
git merge branch_name_to_merge_with //merge two branches 


Easy way to push code to remote repository for 1st time
-------------------------------------------------------
0. first open cmd/command prompt in the folder in which your uploading files are there
1. git init
2. git add .
3. git status
4. git commit -m "comment"
5. git remote add origin repository_link
6. git push -u origin mater/any other branch name


Push code when updated the file and pushing 2nd time
----------------------------------------------------
0. git add .
1. git status
2. git commit -m "comment"
3. git push
